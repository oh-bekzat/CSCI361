enum Pages {
  home,
  info,
  profile,
}

enum Services {
  fueling,
  mainten,
}

enum ProfileTypeInfo {
  role,
  email,
  address,
  phone,
  iin,
}

enum VehicleTypeInfo {
  licensePlate,
  capacity,
  tankVolume,
  mileage,
  lastMaintainedDate,
  lastFueledDate,
}

enum RouteStatus {
  awaiting,
  assigned,
  completed,
  started,
}
